log = require('../lib/index')(module)

model = require './model'

log.wtf "Some warning with {number}", { number: 42 }
log.wtf "Some warning"
model.stuff()

console.log "logged via console"
console.warn "warning with number %d, string %s and json %j", 24, 'abc', [1,2,3,{ foo: 'bar'}]
require('util').debug 'debugging message'
