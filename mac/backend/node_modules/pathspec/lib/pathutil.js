// Generated by IcedCoffeeScript 1.3.3f
(function() {

  exports.removeTrailingSlash = function(path) {
    if (path === '/') {
      return path;
    } else if (path[path.length - 1] === '/') {
      return path.substr(0, path.length - 1);
    } else {
      return path;
    }
  };

  exports.addTrailingSlash = function(path) {
    if (path === '') {
      return path;
    } else if (path[path.length - 1] !== '/') {
      return "" + path + "/";
    } else {
      return path;
    }
  };

  exports.removeLeadingSlash = function(path) {
    if (path[0] === '/') {
      return path.substr(1);
    } else {
      return path;
    }
  };

  exports.addLeadingSlash = function(path) {
    if (path[0] !== '/') {
      return "/" + path;
    } else {
      return path;
    }
  };

}).call(this);
