# Dummy Service

Used for one-off commands and to make sure that the RPC system works.

    exports.create = ->
      {
        name: 'dummy'
        ping: (arg, done) ->
          done(null, arg)
        fail: (arg, done) ->
          done(new Error "Mesa gettin' very very scared")
        crash: (arg, done) ->
          throw new Error "Mesa day startin pretty okee, then BOOM!"
      }
