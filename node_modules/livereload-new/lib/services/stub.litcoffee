# Stub Service

Ignores legacy commands during the transition period.


## Preamble

    debug = require('debug')('livereload:stub')


## The Service

    exports.create = (name, commands) ->
      new StubService(name, commands)

    class StubService
      constructor: (@name, commands) ->
        for command in commands
          @[command] = @_ignore.bind(@, command)

      _ignore: (command, arg, callback) ->
        debug "Ignoring %s with %j", command, arg
        callback()
