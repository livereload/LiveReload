declare module "livereload-server" {

    export = LRWebSocketServer;
    class LRWebSocketServer {
        constructor(options);
        on(name: string, handler: Function);
        listen(callback: (err) => void);
        close();
    }

}
