soa = require('livereload-soa')

class DummyService extends soa.ServiceImpl
  dispose: (callback) ->
    callback()

module.exports = (carrier, options={}) ->
    options.name or= 'dummy'
    return new DummyService(carrier, options)
