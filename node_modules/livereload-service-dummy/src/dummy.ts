/// <reference path="../../livereload-soa/index.d.ts" />
import soa = require('livereload-soa');

class DummyService extends soa.ServiceImpl {

}

export = createDummyService;
function createDummyService(carrier, options={}): soa.Service {
    options.name || (options.name = 'pingpong');
    return new DummyService(carrier, options);
}
