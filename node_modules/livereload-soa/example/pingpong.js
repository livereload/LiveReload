var __extends = this.__extends || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    __.prototype = b.prototype;
    d.prototype = new __();
};
var soa = require('livereload-soa');

var PingPongService = (function (_super) {
    __extends(PingPongService, _super);
    function PingPongService() {
        _super.apply(this, arguments);
    }
    PingPongService.prototype.initialize = function () {
        setInterval(this.tick.bind(this), 1000);
    };

    PingPongService.prototype.on_ping = function (message, callback) {
        callback(null, 42);
    };

    PingPongService.prototype.tick = function () {
        this.send({ command: 'pong', service: 'whatever' });
    };
    return PingPongService;
})(soa.ServiceImpl);

function endpoint(carrier) {
    return new PingPongService(carrier, { name: 'pingpong' });
}
exports.endpoint = endpoint;

