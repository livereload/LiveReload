
  exports.publish = function(_arg) {
    var author, body, comment, postId;
    postId = _arg.postId, author = _arg.author, body = _arg.body;
    comment = {
      postId: postId,
      author: author,
      body: body,
      id: DB.comments.length
    };
    DB.put('comments', comment);
    return comment.id;
  };
