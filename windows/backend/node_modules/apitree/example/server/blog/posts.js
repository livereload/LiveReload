
  exports.publish = function(_arg) {
    var authorId, body, post, title;
    authorId = _arg.authorId, title = _arg.title, body = _arg.body;
    post = {
      authorId: authorId,
      title: title,
      body: body,
      id: DB.posts.length
    };
    DB.put('posts', post);
    return post.id;
  };
