
  exports.create = function(_arg) {
    var name, password, user;
    name = _arg.name, password = _arg.password;
    user = {
      name: name,
      password: password,
      id: DB.users.length
    };
    DB.put('users', user);
    return user.id;
  };
